#!/usr/bin/env bash
set -euo pipefail

chown -R 1000:1000 "${APP_DATA_DIR}/data/config"
chown -R 1000:1000 "${APP_DATA_DIR}/data/secrets"
chown -R 1000:1000 "${APP_DATA_DIR}/data/data"

echo "$LDAP_JWT_SECRET" > $APP_DATA_DIR/data/secrets/ldap_secret
